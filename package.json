{
  "name": "flight-deck",
  "description": "A deck of cards with shuffling.",
  "version": "0.1.0",
  "devDependencies": {
    "karma": "~0.8.5"
  },
  "engines": {
    "node": ">=0.8.0"
  },
  "scripts": {
    "test": "./node_modules/.bin/karma start --single-run --browsers PhantomJS"
  }
}
